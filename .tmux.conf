# :source-file ~/.tmux.conf to reload tmux config

set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# clear history of tmux with ctrl+k
bind -n C-k send-keys -R \; send-keys C-l \; clear-history

set -g mouse on
